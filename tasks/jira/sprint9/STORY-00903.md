# STORY-00903: expo-notifications 前端集成

**Epic**: E-007 后台长任务无感回归
**Sprint**: Sprint 9
**Points**: 2
**Owner**: Frontend Dev
**Status**: Done (native 部分等下次 build 生效)

## Description
As the frontend, I want to request permission, obtain an Expo Push Token, register it with the backend, and handle notification taps that route to the correct screen.

## Expected Result
- App 冷启动时 initPushNotifications() 尝试拿权限 + token
- 模块缺失（当前 build 没 expo-notifications）时静默降级
- 点通知（job_ready）→ 跳 Episode 屏
- 点通知（job_failed）→ 跳 Import 屏
- app.json 配 plugin + iOS UIBackgroundModes remote-notification

## Acceptance Criteria
- [x] lib/pushNotifications.ts 动态 import 保护
- [x] 权限拒绝/模块缺失 App 不崩溃
- [x] token 拿到后 POST /api/push/register
- [x] 通知点击 handler 挂在 _layout.tsx
- [x] 冷启动通知（getLastNotificationResponseAsync）也能跳转
- [x] app.json plugin 配好（等下次 native build 生效）

## Notes (等下次 build)
- 需 EAS build 后 expo-notifications 原生模块才真正激活
- iOS 会弹权限申请弹窗
- Push token 会真正产生并上报后端
- **本 Sprint 不 build，代码 ready 状态，与 App 图标等一起打包**

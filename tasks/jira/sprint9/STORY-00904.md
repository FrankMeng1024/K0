# STORY-00904: 后端 Expo Push 发送 + token 存储

**Epic**: E-007 后台长任务无感回归
**Sprint**: Sprint 9
**Points**: 2
**Owner**: Backend Dev
**Status**: Done

## Description
As the backend, when a URL import job completes, I want to push a notification to the user's device via Expo Push Service, so they know without opening the app.

## Expected Result
- push_tokens 表存储 (user_id, token) 关系
- POST /api/push/register 让前端注册 token
- POST /api/push/test 测试推送（开发用）
- import URL pipeline 完成时自动调 Expo Push
- 推送失败不影响业务 job 状态

## Acceptance Criteria
- [x] migration 005_push_tokens.sql 建表
- [x] pushService.js 封装 Expo Push API 调用，30s 超时保护
- [x] 无效 token 过滤（非 ExponentPushToken/ExpoPushToken 前缀）
- [x] push router 挂载 /api/push
- [x] importUrl.js pipeline 完成/失败时触发推送
- [x] 推送失败 logger.warn 但不 throw

# STORY-00001: Expo 项目骨架 + 4 字体加载 + Home 空壳

**Epic**: E-007 Foundation（CR-001 后重写）
**Sprint**: Sprint 1
**Points**: 3
**Owner**: Frontend + DevOps
**Status**: Done

## Description
As Frontend/DevOps, I want to 初始化 Expo TypeScript 项目 + Expo Router + NativeWind + 4 Google Fonts 加载，Home 屏幕先渲染一个"Hello K0"，so that 后续开发有可跑起来的 App 骨架。

## Expected Result
- `cd C:\ClaudeCodeProjects\K0 && npx create-expo-app@latest . --template blank-typescript` 初始化（在 K0 根目录建 App）
- 配置 `app.json`：bundle identifier `com.yiiling.k0`, name "K0", slug "k0"
- 安装 Expo Router + NativeWind + Zustand + React Query + react-native-svg + @react-native-async-storage/async-storage + expo-font + expo-haptics + react-native-safe-area-context
- `app/_layout.tsx` 加载 4 字体（Bagel Fat One / Rubik Bubbles / Sniglet / Fraunces）
- `app/index.tsx` Home 屏幕：显示 "Hello K0" 用 Bagel Fat One
- 用户在 Git Bash 里跑 `bash scripts/start-app.sh` → `expo start` 启动开发服务
- 用户在 Git Bash 里跑 `bash scripts/start-web.sh` → `expo start --web` 起 web 版

## Acceptance Criteria
- [ ] `package.json` 存在，含 dev/build/start/web/test 脚本
- [ ] `app.json` bundle identifier 为 `com.yiiling.k0`
- [ ] 4 字体成功加载（打开 App/Web 后 "Hello K0" 是 Bagel Fat One 胖字母）
- [ ] `bash scripts/start-web.sh` 起服后 http://localhost:8081 可访问
- [ ] Playwright 可截图两个 viewport 无 console error
- [ ] `.env.example` 完整（EXPO_PUBLIC_API_URL）

## Dependencies
- Blocks: STORY-00003, SPIKE-005

## Notes
- 项目根目录（C:\ClaudeCodeProjects\K0）直接是 Expo 项目根
- backend 单独在 `backend/` 子目录（STORY-00002）
